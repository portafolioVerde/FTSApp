<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/list_item_perfil"
        android:icon="@drawable/ic_arrow"
        android:title="@string/perfil_title"
        app:showAsAction="never"/>

    <item android:id="@+id/list_item_menu"
        android:icon="@drawable/ic_arrow"
        android:title="@string/menu_title"
        app:showAsAction="never"/>

    <item android:id="@+id/list_item_cerrar"
        android:icon="@drawable/ic_arrow"
        android:title="@string/cerrarsesion_title"
        app:showAsAction="never"/>

</menu>